import styled from 'styled-components';

const RegisterLine = () => {
  return <RegisterLineStyle />;
};

export default RegisterLine;

const RegisterLineStyle = styled.div`
  background: #ddd;
  margin: 8px 0;
  height: 1px;
`;
