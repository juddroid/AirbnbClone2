import styled from 'styled-components';
import UserButton from './UserButton';

const User = () => {
  return (
    <UserStyle>
      <UserButton />
    </UserStyle>
  );
};

export default User;

const UserStyle = styled.div``;
