const size = {
  XXL: '1440px',
  XL: '1128px',
  L: '950px',
  M: '744px',
  S: '375px',
};

const theme = {
  XXL: `(min-width: ${size.XXL})`,
  XL: `(min-width: ${size.XL})`,
  L: `(min-width: ${size.L})`,
  M: `(min-width: ${size.M})`,
  S: `(min-width: ${size.S})`,
};

export default theme;
